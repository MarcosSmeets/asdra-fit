# React Native / Expo Metro bundler works most reliably with a hoisted layout.
node-linker=hoisted

# Expo/Nest presets frequently ship loose peer ranges; don't fail install on them.
strict-peer-dependencies=false
auto-install-peers=true
